# 代码审查工作台 · 设计稿概述

## 完成内容
在 Ardot 画布上完成了一帧 1440×960 的桌面端代码审查工作台设计稿（文件：代码审查工作台，ID 722497116688493），围绕"代码审查过程实时呈现"展开。

## 视觉系统
- **风格基底**：Notion 式编辑风格（选定模板 00005）—— 浅色画布 #FFFFFF + 浅灰表面 #F6F5F4 + 紫色主色 #5645D4 + 柔和粉彩语义色
- **视觉锚点**：深海军蓝 #0A1530 代码审查面板，与 Notion 风格的浅色编辑外壳形成主次对比
- **字体**：Inter（UI 文本）+ JetBrains Mono（代码与数字）
- **圆角**：按钮 8px、卡片 12px、状态徽章 999px（pill）
- **阴影层级**：卡片轻量阴影 + 代码面板双层阴影（深投影 + 紫色环境光）

## 信息架构（契合代码/安全审查场景）
- **侧边栏**：品牌区 → 工作区导航（总览/实时审查/检查清单/代码扫描/漏洞跟踪/工具矩阵/报告导出）→ 项目分组（mall-ecommerce 激活、user-center、pay-service）→ 用户信息
- **顶栏**：仓库 / PR 面包屑 + 审查进行中实时徽章（呼吸点发光）+ 搜索 + 暂停 / 导出报告
- **左列（主）**：
  - 六阶段审查流水线步进器（准备·信息收集 → 静态代码分析 → 密钥与依赖扫描 → 人工+AI 语义审查 → 动态/运行时测试 → 汇总·报告·复测）
  - 代码审查面板（深海军蓝）：文件标签栏、14 行带行号的语法着色 TypeScript 登录代码、两处 P1 / P0 行内发现（硬编码密钥 + SQL 拼接注入），每处配独立标注卡描述与"标记误报 / 加入整改单"操作；底部面板状态条
- **右列（实时）**：
  - 实时审查流：6 条带时间戳的 LIVE 事件，包含进行中的 AI 语义审查与流式输出子行
  - 审查统计：68% 进度条 + P0×1 / P1×3 / P2×4 / P3×1 严重度分布
  - 工具执行状态：Semgrep / gitleaks 已完成，CodeQL 运行中 45%，OWASP ZAP 排队中

## 设计原则落地
- **主导区域**：代码面板以最大体量与最深色形成视觉锚点
- **实时性传达**：紫色发光呼吸点、LIVE 徽章、运行中旋转圆弧、流式输出副文本、运行中行使用 lavender 高亮
- **状态可见性**：每个工具、每个阶段都有明确状态（完成 / 进行中 / 待执行）
- **信息层级**：使用 4 个色阶（紫主色 / 粉彩语义色 / 中性灰阶）区隔品牌、状态、辅助信息
- **约束即装饰**：仅保留必要元素，无装饰性分隔线，所有图标均使用真实 SVG

## 5 页导航合集扩展（同套设计语言）

将单页「实时审查」扩展为完整的 7 项导航工作台合集（Ardot 文件 ID 722522304544442，1440×960 × 5 帧水平并排）：

| # | 页面 | Ardot 节点 | 信息架构 |
|---|---|---|---|
| 01 | 实时审查 | 722497116688493 · 3:1（原独立设计稿） | 六阶段流水线 · 深海军蓝代码面板 · · 实时审查流 · 工具执行状态 |
| 02 | 检查清单 | 722522304544442 · 3:1 | OWASP 64 项 · 目标/严重度过滤 · 已通过 38 / 待审 22 · 分类完成度 · 近期活动 |
| 03 | 代码扫描 | 3:207 | 21 条规则 · 实时进度 · 命中流（SCANNING 徽章 + LIVE 流式条目）|
| 04 | 漏洞跟踪 | 3:413 | P0/P1/复测/已修复 KPI · 5 条漏洞行 · 状态分布柱图 · 责任人负载 · 逾期提醒 |
| 05 | 工具矩阵 | 3:619 | 8 工具 2×4 网格（SAST/DAST/SCA，含运行/排队/未接入态）+ 接入概览 + 最近执行 |
| 06 | 报告导出 | 3:825 | 3 模板（完整审计·高管·整改，含激活态）+ 文档预览（P0/P1/P2/P3 统计块 + 章节列表）+ 导出历史 |

**一致性设计要点**
- 同一浅色 Notion 式外壳 + 紫色 #5645D4 + 深海军蓝代码面板配色
- 侧栏 7 项导航 + 项目分组 + 用户信息区在所有页面结构相同，仅高亮项切换
- 顶栏布局一致（面包栏 · 状态徽章 · 搜索 · 主次按钮），文案随页面切换
- 主列 847px · 右列 336px · 间距 24px · 卡片 12px 圆角 · 阴影统一
- P0–P3 语义色在所有页面复用：玫红 / 桃 / 米黄 / 天蓝
- 16 类 SVG 图标在所有页面复用（仓库 / 列表 / 漏洞 / 工具 / 文档 / 搜索 / 圆点等）

**Ardot 文件**：722522304544442（5 帧在同一画布，便于整体预览与评审）

## 应用生成（HTML SPA）

将 6 页设计稿整合到单 HTML 应用 `代码审查工作台_v2.html`：

- **架构**：单文件 SPA，左侧 232px 侧栏 + 主区域（顶栏 64px + 内容 24px padding）。侧栏导航切换不同 page 容器，顶栏文案/状态徽标/搜索/按钮随页面动态切换。
- **页面**：总览（占位）/ 实时审查 / 检查清单 / 代码扫描 / 漏洞跟踪 / 工具矩阵 / 报告导出，共 7 个 page 容器通过 `data-page` 标识，`display:flex/none` 切换
- **设计 token 严格对齐**：所有色值、字号、圆角、间距、阴影与画布设计稿一致（侧栏 #F6F5F4、主色 #5645D4、P0–P3 粉彩、卡片 12px 圆角）
- **保留实时审查页全部交互**：审查进行中呼吸点 · 68% 进度条 0→载入动画 · 实时时钟 · 流式打字机 · 暂停/继续 · 标记误报/加入整改单 toast
- **新增 5 页交互**：
  - 检查清单：严重度/目标类型 chip 切换、整体进度 38 大数字递增动画
  - 代码扫描：81% 进度条载入、规则命中流式 LIVE 卡片
  - 漏洞跟踪：4 KPI 卡片、状态分布柱图、逾期提醒
  - 工具矩阵：8 工具 2×4 网格、接入概览 5/8、添加工具点击反馈
  - 报告导出：3 模板切换（active 描边 + 渐变背景）、格式/范围单选切换、复测建议开关可切换
- **通用交互**：toast 队列、键盘焦点可达性、`<1280px` 兜底、Notion 式悬停态

**输出文件**：`C:\Users\26717\WorkBuddy\代码审查\代码审查工作台_v2.html`（约 1900 行，单文件自包含，1440×960 基准 + 自适应窗口宽度）

**视觉验证**：1440×960 视口下逐页截图比对 6 张设计稿 — 还原度极高，色块/字号/间距/对齐全部对齐。
## 项目切换器（侧栏 · 项目分组联动）

侧栏「项目」分组的 3 个项目（mall-ecommerce / user-center / pay-service）点击后全局联动切换内容：

- **数据层**：`PROJECTS` 对象按项目存储全部页面数据 —— 面包屑 PR、状态徽标、代码文件标签与完整语法着色代码（含行内发现卡）、LIVE 事件流、工具执行状态、扫描统计与实时命中、检查清单标题/计数、报告预览元信息
- **切换函数**：`applyProjectData(key)` 统一更新 DOM + 同步 `PAGES` 配置表（顶栏面包屑/徽标随项目走，切页不回退），并重放当前页顶栏配置
- **每项目差异化剧本**：
  - mall-ecommerce · PR #482 认证模块重构 — TypeScript login.ts，P0 SQL 注入 + P1 硬编码密钥，扫描 81%
  - user-center · PR #156 密码重置流程升级 — TypeScript reset.ts，P1 令牌过期过长 + P1 无速率限制，扫描 76%
  - pay-service · PR #89 支付签名验签重构 — Go sign.go，P1 签名重放风险，扫描 92% · 状态全绿
- **验证**：3 项目 × 实时审查/代码扫描页截图回归全绿，切换时 toast「已切换至项目 · ×××」反馈

## 守护中枢（v0.3 · 并入工作台第 8 个导航页）

把「演示工作台」升级为真正的守护服务（`guardian/`，Python 单文件零依赖 + 驾驶舱 HTML）：

- **双通道守护**：中枢探测（online/latency/tls/content/security 五探针）+ 客户端上报 API（网站 / 小程序 / App 心跳与异常），外加代理采集 `agent_collect.py`
- **v0.3 能力（对标 uptime-kuma / gatus / healthchecks）**：
  - 通知渠道：Webhook / 企业微信 / 钉钉 / 飞书 / Server酱，状态翻转异步投递 + 测试通知
  - 可用率 24h / 7d / 30d（事件时间线精确重算，跨重启不丢）
  - 维护窗口（UI 横幅公告 / 通知静默 / 可用率豁免）
  - 登记自测 `/api/v1/validate`、Token 轮换、目标启停与注销
  - SVG 可用率徽章 + 公开只读状态页 `/public`
  - 界面演示降级：连不上中枢自动切内置演示数据，可一键重连（GitHub Pages 直开可演示）
- **形态**：`代码审查工作台_v2.html` 侧栏第 8 项「守护中枢」内嵌版 + `guardian/guardian_ui.html` 独立驾驶舱版（同视觉语言）；设计文档 `守护工作台_设计方案.md`
- 冒烟产物与截图见 `.workbuddy/smoke-test/`
