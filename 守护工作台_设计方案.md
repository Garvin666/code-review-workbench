# 守护工作台（Guardian Workbench）设计方案

> 版本：v0.3（2026-09-05）· 原 v0.1 草案内容全部落地并已并入 `代码审查工作台_v2.html` 第 8 个导航页；本文件保留原始设计并追加 v0.3 升级实录。
> 定位：把「代码审查工作台」的形态从演示界面升级为**守护中枢**——对外分发 API，让网站 / 小程序 / App 接入后被守护。

---

## 1. 项目概述

### 1.1 目标

将现有工作台（`代码审查工作台.html` / `_v2.html`）沉淀下来的界面形式，转化为一个**真正能跑起来的守护器**：它守护网站、小程序、App 的服务端与客户端运行状态，实时在工作台上呈现，异常时告警。

### 1.2 核心思想：工作台 = 守护中枢（分发 API）

与常见"单向探测监控"不同，本方案的关键设计是：**工作台自身对外分发守护 API**。

```
        ┌──────────────────────────────────────────────┐
        │              守护工作台（守护中枢）              │
        │                                              │
        │   ┌──────────┐   ┌─────────┐   ┌─────────┐   │
        │   │ 上报 API │   │ 探测引擎 │   │ 判定引擎 │   │
        │   │ /v1/*    │   │ 主动拉取 │   │ 状态机  │   │
        │   └────┬─────┘   └────┬────┘   └────┬────┘   │
        │        │             │             │        │
        │   ┌────┴─────────────┴─────────────┴────┐   │
        │   │          存储 targets / events / 历史  │   │
        │   └──────────────────┬──────────────────┘   │
        └──────────────────────┼──────────────────────┘
                               │
          ┌────────────────────┼────────────────────┐
          ▼                    ▼                    ▼
     ┌──────────┐       ┌───────────┐        ┌──────────┐
     │ 被守护方  │       │ 被守护方   │        │ 守护驾驶舱 │
     │ (主动上报) │       │ (被动被查)  │        │ 界面(复用  │
     │ 小程序/App│       │ 网站/接口  │        │ v2 壳子)  │
     └──────────┘       └───────────┘        └──────────┘
```

- **上报通道（Channel A · 分发 API）**：被守护方主动调用工作台 API 上报心跳、性能、错误、安全事件。解决"外部脚本永远探测不到客户端内部状态"的死结——**只有小程序/App 自己知道自己在卡顿/报错**。
- **探测通道（Channel B · 主动拉取）**：工作台按目标清单定时探测网站/接口的可用性、延迟、TLS 证书、内容指纹、安全响应头。
- 两条通道的数据汇入**同一个判定引擎**（状态机 + 阈值 + 去抖），统一事件流、统一告警。

### 1.3 为什么能复用现有工作台

v2 的壳子本质是一套"监控驾驶舱骨架"，界面元素可直接映射：

| 现有 v2 元素 | 守护驾驶舱中的角色 |
|---|---|
| 左侧 232px 侧栏（页面导航） | 守护目标列表 / 分组导航 |
| 顶栏状态徽标 | 全局守护状态（正常绿 / 告警红） |
| 主区 KPI 大数字 | 守护目标数 / 在线数 / 告警数 / 今日事件数 |
| 检查清单 / 扫描进度 | 每目标探针健康明细与最近巡检时间 |
| 事件流 / toast 队列 | 告警与恢复事件流 |
| 暂停 / 恢复按钮 | 单目标守护开关（暂停不再告警） |

---

## 2. 总体架构

### 2.1 组件清单（单进程，零第三方依赖）

| 组件 | 职责 | 说明 |
|---|---|---|
| `HTTPServer`（http.server 标准库） | 提供 HTTP API + 托管驾驶舱界面 | 默认绑定 `127.0.0.1:8700`，`--host 0.0.0.0` 可开放局域网 |
| API 路由层 | 实现 `/api/v1/*`，token 鉴权 | 分发守护能力的对外入口 |
| 上报接收器 | 处理 /report /event | 校验 token、写事件与历史 |
| 探测引擎（后台线程） | 按 targets.json 定时执行各探针 | 每目标独立 interval，5~60s |
| 判定引擎（状态机） | ok → warn → down → recovered | 连续 N 次失败才降级，防抖动 |
| 告警器 | 变更时产生事件 + 推送前端 | 徽标变色 + 前端系统通知 |
| 存储层 | JSON / JSONL 文件读写 | 见 §4 |
| 驾驶舱界面 `guardian_ui.html` | 状态轮询与可视化 | 复用 v2 设计语言，独立新文件 |

### 2.2 线程模型

```
主线程:    HTTP 服务（接受上报 + 托管页面 + 前端轮询）
守护线程1: 探测调度器（遍历 targets → 按 interval 执行探针组）
守护线程2: 告警 / 事件广播（可选，事件由判定引擎同步写盘，前端轮询获取）
```

单文件即可承载：目标规模小（数十个），无高并发需求，标准库 `ThreadingHTTPServer` 足够。

### 2.3 运行形态

- 本地演示 / 开发：`python guardian_server.py` → 浏览器打开 `http://127.0.0.1:8700`
- 真实守护内网/公网服务：`python guardian_server.py --host 0.0.0.0 --port 8700`，并配置 Windows 防火墙放行；被守护方填写可到达的中枢地址
- 一键启动脚本：`start.bat`（含静默启动 + 自动开浏览器），沿用本项目工作约定

---

## 3. 双通道数据流

### 3.1 上报通道（Channel A）— 被守护方 → 工作台

被守护方接入成本 = 一段 ≤15 行的上报代码片段（Python / JS / curl 三选一），定时调用：

```
POST /api/v1/report
{
  "token": "<目标 token>",        // 登记目标后领取，服务端只存哈希
  "metric": "online",             // online | latency_ms | error | security | content_hash
  "ok": true,                     // 本次是否正常
  "value": 128,                   // 可选数值（如延迟 ms）
  "detail": "{}",                 // 可选补充信息
  "ts": 1736064000000             // 可选，缺省用服务端时间
}
```

`POST /api/v1/event` 用于一次性异常事件（崩溃、报错、攻击告警），字段同上，`metric` 取 `runtime_error` / `crash` / `attack` 等。

**典型接入场景**

- 小程序：在 `app.onError` / 网络层拦截器里埋点，定时上报心跳 + 页面错误
- App：启动器起一个后台线程，每 60s 上报心跳；崩溃回调里上报 `crash` 事件
- 网页：`window.addEventListener('error'/'unhandledrejection')` + `navigator.sendBeacon` 兜底

### 3.2 探测通道（Channel B）— 工作台 → 被守护方

对不需要（或无法）改造上报代码的目标，直接由中枢探测。探针类型与判定见 §5。

### 3.3 代理采集（对老项目最友好的通道）

对"完全不能改造"的目标，提供 `agent_collect.py`：在中枢侧（或目标侧）运行，代替目标执行采集动作（跑命令、读日志文件、请求内网接口），再把结果**以目标的身份**上报给中枢 API。这样目标侧无需任何改造，只需在中枢登记时把 `source: "agent"` 的目标绑到采集脚本上。

```
guardian 中枢 (127.0.0.1:8700)
      ▲ 上报（代理身份）
      │
agent_collect.py  ← 定时执行采集动作（读日志/调接口/跑命令）
      ▲
老项目（不可改造）
```

---

## 4. 数据模型与存储

### 4.1 目录结构（规划）

```
guardian/
├─ guardian_server.py      # 主程序（单文件：HTTP + 引擎 + 存储 + API）
├─ guardian_ui.html        # 守护驾驶舱界面（复用 v2 设计语言，独立文件）
├─ agent_collect.py        # 代理采集脚本（替不可改造目标上报）
├─ start.bat               # 一键启动（静默 + 开浏览器）
├─ snippets/
│  ├─ report_python.py     # 被守护方 Python 上报片段
│  ├─ report_js.js         # 被守护方 JS 上报片段
│  └─ report_curl.txt      # curl 示例
├─ data/
│  ├─ targets.json         # 目标清单（token 只存哈希）
│  ├─ events.jsonl         # 事件流（追加写）
│  └─ history.jsonl        # 指标历史（追加写，供曲线）
└─ README.md               # 使用说明
```

### 4.2 targets.json 结构

```json
[
  {
    "id": "web-shop",
    "name": "商城官网",
    "type": "website",                    // website | api | app | miniprogram | agent
    "url": "https://example.com",
    "source": "probe",                    // probe=中枢探测 | report=客户端上报 | agent=代理采集
    "token_hash": "sha256:...",           // 上报通道鉴权，只存哈希；source=probe 可空
    "probes": ["online", "tls", "content", "security"],
    "interval_s": 30,
    "timeout_ms": 3000,
    "thresholds": {
      "fail_to_warn": 1,
      "fail_to_down": 3,
      "slow_ms": 2500,
      "tls_days": 30,
      "content_strict": false
    },
    "content_baseline_sha256": null,      // 首次探测后写入基线，null 表示自动建基线
    "expected_headers": [
      "Strict-Transport-Security",
      "Content-Security-Policy",
      "X-Content-Type-Options"
    ],
    "enabled": true,
    "created_at": "2026-09-05T00:00:00+08:00"
  }
]
```

### 4.3 事件与历史

- `events.jsonl`：一行一个事件 `{ts, target_id, level, type, message, detail}`；**追加写**，与工作台既有约定一致
- `history.jsonl`：`{ts, target_id, metric, ok, value}`，保留最近 ~7 天，供驾驶舱曲线与统计
- 事件类型全集（统一进入徽标判定与事件流）：

| type | 触发 | level |
|---|---|---|
| `target_down` | 状态机进入 down | critical |
| `target_recovered` | 从 down/warn 恢复 ok | info |
| `probe_timeout` | 请求超时 | warn |
| `perf_slow` | 超过 slow_ms | warn |
| `tls_expiring` | 证书剩余 < tls_days | warn |
| `content_changed` | 内容哈希与基线不一致 | warn/info |
| `security_header_missing` | 缺少期望安全头 | warn |
| `client_error` | 客户端上报运行时错误 | warn |
| `client_crash` | 客户端上报崩溃 | critical |
| `client_attack` | 客户端上报安全攻击 | critical |

---

## 5. 探测引擎（Channel B 探针组）

每个探针独立执行、独立判级，探针全部通过目标才为 ok。全部用 Python 标准库实现（`urllib` / `ssl` / `socket` / `hashlib`）。

| 探针 | 探测内容 | 判定 | 对应守护需求 |
|---|---|---|---|
| `online` | GET/HEAD 目标 URL，状态码 2xx/3xx | 失败计数累计 → warn/down | 在线可用性 |
| `latency` | 请求往返耗时 | > slow_ms → `perf_slow` 事件 | 性能 |
| `tls` | 建立 TLS 连接，读证书 notAfter | 剩余天数 < 阈值 → 告警 | 可用性（证书到期即不可用） |
| `content` | 抓正文算 sha256 | 与 baseline 不一致 → `content_changed` | 内容/版本变更 |
| `security` | 检查响应安全头；可选探测常见泄露路径 | 缺头 / 泄露 → 告警 | 安全 |

安全探针（零依赖可实现范围）：
1. 期望响应头缺失检查（HSTS / CSP / X-Content-Type-Options / X-Frame-Options）
2. TLS 最低版本与证书链（`ssl` 握手结果）
3. 可选：探测 `/.env`、`/.git/config`、`/admin` 等常见暴露路径，存在且返回 200 记 `security_leak`

---

## 6. 判定引擎与状态机

```
                   连续 ≥ fail_to_down 次失败
    ┌──────────┐ ────────────────────────────► ┌──────────┐
    │   ok     │                               │   down   │
    │  (绿)    │ ◄──────────────────────────── │  (红)    │
    └──────────┘       单次成功自动恢复         └──────────┘
         │  ▲
         │  │ 连续 ≥ fail_to_warn 次失败(去抖窗口内)
         ▼  │ 单次成功即回 ok
    ┌──────────┐
    │   warn   │  (黄)  ⚠ 首次异常、慢请求、证书临期
    └──────────┘
```

- **去抖原则**：单次失败不立即判 down，防止网络抖动误报；达到阈值才切换并写事件
- **恢复**：任一成功探测即回 `ok`，写 `target_recovered`（info），不再重复告警
- **客户端上报（Channel A）降级语义**：`ok:false` 的上报相当于一次失败，同样累计计数
- 状态落盘快照供驾驶舱秒级读取，无需每次全量算历史

---

## 7. 告警设计（零依赖约束下的取舍）

约束：主程序不引入第三方库 → 系统级桌面通知不依赖 `plyer`/`win10toast`。

| 告警层 | 实现 | 触发 |
|---|---|---|
| 界面内告警 | 顶栏状态徽标变色（绿→黄→红）+ 事件流高亮 + 闪烁 + 声音 | 事件实时写入后由前端轮询拉到 |
| 系统级桌面通知 | 驾驶舱页面使用浏览器 **Notification API**（用户首次授权一次）；页面处于后台也可弹系统通知 | 仅 critical / warn 事件 |
| 轮询机制 | 驾驶舱每 3s `fetch /api/v1/status` + 增量拉取新事件 | 前端主动 |

浏览器通知是唯一"零依赖 + 跨平台"的桌面通知路径；若未来需要脱离浏览器常驻提醒，再评估加 `plyer`（届时作为可选依赖，不破坏单文件原则）。

---

## 8. 分发 API 契约（v1）

鉴权：除 `GET /api/v1/status`、`GET /api/v1/ui` 等本地只读接口外，上报/写接口要求请求头 `X-Guardian-Token: <token>`，服务端比对哈希。本机演示默认关闭严格鉴权（`--no-auth` 等价于绑定 127.0.0.1 时的默认信任），对外开放时强制开启。

| 方法 | 路径 | 用途 | 返回 |
|---|---|---|---|
| POST | `/api/v1/report` | 被守护方上报心跳/指标 | `{ok:true, state}` |
| POST | `/api/v1/event` | 被守护方上报异常事件 | `{ok:true}` |
| GET | `/api/v1/status` | 全目标当前状态（含 ok/在线数/告警数/最近事件） | JSON |
| GET | `/api/v1/history?target=&metric=&window=` | 指标历史曲线数据 | JSON |
| GET | `/api/v1/targets` | 登记的目标清单（脱敏，供页面/接入方查看） | JSON |
| POST | `/api/v1/targets` | 新增/登记目标并签发 token | `{ok, token}`（token 仅此一次返回） |
| GET | `/api/v1/snippets` | 取用上报代码片段（页面内展示） | JSON |

响应统一：`{ "ok": bool, "data": ..., "error": "..." }`。

---

## 9. 接入方式（两种都提供）

### 9.1 上报代码片段（对被守护方可改造的场景）

**Python 片段（每 60s 心跳）**

```python
import json, time, urllib.request
HUB = "http://127.0.0.1:8700"          # 守护中枢地址
TOKEN = "<登记目标后领取的 token>"
def report(metric="online", ok=True, value=None, detail=""):
    body = json.dumps({"metric": metric, "ok": ok, "value": value,
                       "detail": detail}).encode()
    req = urllib.request.Request(HUB + "/api/v1/report", body,
                                 {"Content-Type": "application/json",
                                  "X-Guardian-Token": TOKEN})
    urllib.request.urlopen(req, timeout=5)
while True:                            # 业务代码里可放主循环 / 定时器
    report()
    time.sleep(60)
```

**JS / 网页片段**

```js
const HUB = "http://127.0.0.1:8700/api/v1/report";
const TOKEN = "<token>";
function report(ok = true, metric = "online", value, detail) {
  navigator.sendBeacon? navigator.sendBeacon(HUB, new Blob([JSON.stringify(
    {metric, ok, value, detail})], {type: "application/json"}))
  : fetch(HUB, {method: "POST",
      headers: {"Content-Type": "application/json", "X-Guardian-Token": TOKEN},
      body: JSON.stringify({metric, ok, value, detail})});
}
window.addEventListener("error", e => report(false, "runtime_error", 0, e.message));
```

**curl 示例（脚本/定时任务用）**

```bash
curl -s -X POST http://127.0.0.1:8700/api/v1/report \
  -H "Content-Type: application/json" -H "X-Guardian-Token: <token>" \
  -d '{"metric":"online","ok":true}'
```

### 9.2 代理采集脚本（对不可改造目标）

`agent_collect.py --target web-shop --cmd "..."`：周期性执行采集命令或读取日志文件，把结果转为 `{ok, value, detail}` 后以该目标身份上报。中枢侧配置 `source:"agent"` 的目标即可。

---

## 10. 驾驶舱界面设计（guardian_ui.html）

复用 v2 的视觉语言（Notion 编辑外壳 + 深海军蓝 `#0A1530` 代码/状态区 + 紫 `#5645D4` 品牌色），但完全独立文件，**不动现有 v2**。

| 区域 | 内容 |
|---|---|
| 侧栏 | 「守护总览」+ 每个守护目标一个条目（名字 + 状态圆点 + 最近事件数），支持分组 |
| 顶栏 | 全局状态徽标（守护中 / N 个告警）+ 暂停/恢复全局守护 + 登记目标按钮 + 实时时钟 |
| 总览区 | KPI 大数字：目标总数 / 在线 / 告警中 / 今日事件；通道分布（上报 vs 探测 vs 代理） |
| 目标详情 | 探针健康明细（每条绿/黄/红）、最近巡检时间、延迟曲线（复用 v2 进度条/曲线动画）、事件列表 |
| 事件流 | 按时间倒序滚动，级别色阶区分，恢复事件绿色置底标记 |
| 登记抽屉 | 表单录入目标 → 调 `/api/v1/targets` → 签发 token → 内嵌「上报代码片段」一键复制 |

前端逻辑：3s 轮询 `/api/v1/status`，增量拉事件；新 critical 事件触发 Notification 系统通知 + toast + 徽标闪烁。骨架、数据与现有 v2 的 page-* 切换、PAGES 配置表思路一致。

---

## 11. 实现计划（分阶段）

| 阶段 | 交付物 | 验收标准 |
|---|---|---|
| M1 骨架 | `guardian_server.py`：HTTP + targets 读写 + 5 个 API + 静态托管 + token 签发 | curl 全接口冒烟通过 |
| M2 探测判定 | 探针组（online/latency/tls/content/security）+ 状态机 + 去抖 + events/history 落盘 | 模拟目标故障→告警→恢复，事件流正确 |
| M3 驾驶舱 | `guardian_ui.html`：状态轮询、目标明细、事件流、登记抽屉、代码片段复制 | 1440×960 截图比对设计稿风格，交互冒烟 |
| M4 接入物料 | snippets 三件套 + `agent_collect.py` + `start.bat` + README | 用示例小程序/网页模拟上报，界面即时变化 |
| M5 联调冒烟 | 造 3 类目标（正常/慢/故障）端到端验证 | 冒烟报告（沿用 `.workbuddy/smoke-test/` 约定） |

### 11.1 里程碑默认值

- 默认端口 `8700`（避开求职助手 8020），默认绑定 `127.0.0.1`
- 轮询间隔默认 30s（可覆盖），前端刷新 3s
- 慢请求阈值 2500ms，超时 3000ms，证书预警 30 天
- 降级阈值：warn = 连续 1 次失败，down = 连续 3 次失败（同一目标 5s 去抖窗口）

---

## 12. 风险与边界（务必诚实标注）

1. **"守护客户端内部"的前提是可到达**：真机上运行的小程序/App 无法访问你开发机的 `127.0.0.1`。要让客户端心跳真实可达，中枢需部署到**目标侧能访问的服务器**（内网或公网），本地只适合开发/演示 + 模拟器场景。这是部署边界，不是代码能绕过的。
2. **安全边界**：中枢对外开放时必须启用 token 鉴权并走 HTTPS 反代；`token` 仅创建时明文返回一次，服务端只存哈希。
3. **Windows 环境**：绑定 `0.0.0.0` 需防火墙放行；脚本沿用 GBK 编码注意事项（避免非 ASCII 路径的 .bat）。
4. **桌面通知**：依赖浏览器 Notification 授权，最小化窗口不弹、完全关闭浏览器不弹——如需 7×24 告警建议后续挂任务计划程序 + 可选 `plyer`。
5. **规模边界**：目标 ≤ 数十、并发上报低频时单线程/双线程完全够用；更大规模再引入队列与独立存储，当前不设计分布式。

---

## 13. 待确认项（v0.3 已全部闭环）

- [x] **中枢是否最终需要部署到公网/内网服务器** —— 是部署边界而非代码问题（见 §12.1）。本地演示用 `127.0.0.1`；真机客户端接入需部署到目标可达地址，文档与 README 已如实标注。
- [x] **演示期目标清单** —— 采用假目标（`--demo` 内置演示站点 :8800 + 演示目标 4 个），同时驾驶舱/独立 UI 均支持**演示数据降级**（连不上中枢自动切换，可重连），GitHub Pages 静态托管直开即可讲解。
- [x] **驾驶舱文件命名** —— 双形态并存：独立 `guardian/guardian_ui.html`（服务端托管 / 可单独打开）+ 并入 v2 侧栏第 8 项「守护中枢」内嵌版（复用同 IIFE 逻辑）。
- [x] **是否作为 v2 的第 7 项入口** —— 已落地为 v2 侧栏第 **8** 项（总览占位 + 7 项功能 + 守护中枢），`PAGES` 配置 + `.page.guardian` 容器 + `.gscope` CSS 隔离。

## 14. v0.3 升级实录（对标 uptime-kuma / gatus / healthchecks 的取舍）

> 需求方要求先调研 GitHub 同类开源项目再升级。调研结论与落地取舍如下：

| 开源标杆可借鉴能力 | 本项目取舍 | 落地 |
|---|---|---|
| uptime-kuma 90+ 通知渠道 | 取 5 种零依赖可达：通用 Webhook / 企业微信 / 钉钉 / 飞书 / Server酱（本质都是 HTTP POST 机器人地址） | `notify.json` + `POST /api/v1/notify`（save/test）+ 状态翻转异步投递；维护窗口内静默 |
| gatus/uptime-kuma 可用率百分比 | 事件时间线精确重算（`target_down`→`target_recovered` 区间扣维护窗口），跨重启不丢 | `/api/v1/status` 返回 `target.uptime`（24h/7d/30d），缓存 10s |
| gatus/uptime-kuma 维护窗口 | 全局/按目标窗口：UI 横幅、通知静默、可用率豁免 | `maintenance.json` + `POST /api/v1/maintenance`（add/delete） |
| uptime-kuma SVG 徽章 | 可用率徽章（按阈值变色），可嵌入 README | `GET /api/v1/targets/<id>/badge.svg?window=7d` |
| uptime-kuma 公开状态页 | 无鉴权只读状态页 | `public_status.html` + `GET /public` `/status` |
| gatus 多条件/单次自测 | 登记前「测试连通」单次探测，不入库 | `POST /api/v1/validate` |
| healthchecks REST API / token | token 轮换（旧立即失效）、目标启停、改参数、注销 | `POST /api/v1/targets/<id>/token/rotate`、`POST/DELETE /api/v1/targets/<id>`；`--auth` 下管理接口校验 `X-Guardian-Admin: <--admin-token>` |
| 接入片段生态 | 补微信小程序片段 + curl 管理示例 | `snippets/report_wx.js`、`report_curl.txt` 扩充 |

**明确不做（过度设计）**：多协议探测（TCP/Ping/DNS/grpc）、SQLite/Postgres 存储、多用户/2FA、Prometheus 导出、K8s 化——与本项目"单文件零依赖、数十目标规模、演示与内网守护"的定位不符。

**存储变更**：`data/` 新增 `notify.json`、`maintenance.json`；`events.jsonl` 兼作可用率重算数据源（追加写）。现有 `targets.json`/`history.jsonl` 结构不变，旧数据无缝兼容。

**UI/接入面变更**：
- `guardian/guardian_ui.html` 全面重写：维护窗口横幅、每目标 24h/7d/30d 可用率、通知设置抽屉（打码 URL）、登记自测、目标详情管理操作（轮换/启停/注销/徽章链接）、**file:// 与 GitHub Pages 直开即演示**（连接失败自动降级 + 重试连接）。
- v2 内嵌守护中枢同步：总览卡片与详情页可用率、维护横幅、登记「测试连通」、微信小程序代码页；演示数据补齐 uptime/maintenance 字段。
- 新增 `public_status.html` 只读状态页。

> 本方案落地后，与现有 `代码审查工作台_v2.html`、`安全审查工作台.html`、`review_pipeline` 相互独立，同目录共存互不干扰。
